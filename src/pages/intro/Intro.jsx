import React from 'react';
import PropTypes from 'prop-types';
import './intro.css';

export const Intro = ({ }) => {
    return (
        <div>Intro Page</div>
    );
};

Intro.propTypes = {

};

Intro.defaultProps = {

};